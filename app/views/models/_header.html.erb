<div class="panel panel-primary">
  <div class="panel-heading">
    Project: <%= @model.name%>
    <% @model_hashtag = @model.hashtags.find_by(reserved: true) %>
    <% if !@model_hashtag.nil? %>
    <% if !current_user.hashtags.find_by(tag: @model_hashtag.tag) %>
      <%= link_to "Follow", { :controller => "users", :action => "hashtag_add", :user_id => current_user.id, :tag => @model_hashtag.tag }, :method => :get, :style => "color: white" %>
    <% else %>
      <%= link_to "Unfollow", { :controller => "users", :action => "hashtag_delete", :user_id => current_user.id, :tag => @model_hashtag.tag }, :method => :get, :style => "color: white" %>
    <% end %>
    <% end %>
  </div>
  <div class="panel-body">
    <p>
      <strong>Model Administrator:</strong>
      <%= @model.user.name %>
    </p>

    <p>
      <strong>Name:</strong>
      <%= @model.name %>
    </p>

    <p>
      <strong>Stored at:</strong>
      <%= @model.path %>
    </p>

    <p>
      <strong>Main script:</strong>
      <%= @model.mainscript %>
    </p>

    <p>
      <strong>Citation Info:</strong>
      <%= @model.citation %>
    </p>

    <p>
      <strong>DOI:</strong>
      <a target="_blank" href="http://doi.org/<%= @model.doi %>"><%= @model.doi %></a>
    </p>

    <p>
      <strong>Hashtags:</strong>
      <% @model.hashtags.each do |h|%>
      <% if h.reserved %>
      <b><%= hashtag_link(h.tag) %></b>
      <% else %>
      <%= hashtag_link(h.tag) %>
      <% end %>
      <% end %>
    </p>

    </p>
  </div>
</div>
